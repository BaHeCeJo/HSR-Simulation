import type { $ZodStringFormats } from "../core/checks.js";
import type * as errors from "../core/errors.js";
import * as util from "../core/util.js";

const error: () => errors.$ZodErrorMap = () => {
  const Sizable: Record<string, { unit: string; verb: string }> = {
    string: { unit: "حرفE, verb: "أفEيحوفE },
    file: { unit: "بايت", verb: "أفEيحوفE },
    array: { unit: "عفE�ر", verb: "أفEيحوفE },
    set: { unit: "عفE�ر", verb: "أفEيحوفE },
  };

  function getSizing(origin: string): { unit: string; verb: string } | null {
    return Sizable[origin] ?? null;
  }

  const FormatDictionary: {
    [k in $ZodStringFormats | (string & {})]?: string;
  } = {
    regex: "فE�خفE,
    email: "بريد إفE�E�روفE�E,
    url: "رابط",
    emoji: "إيفE�جفE,
    uuid: "UUID",
    uuidv4: "UUIDv4",
    uuidv6: "UUIDv6",
    nanoid: "nanoid",
    guid: "GUID",
    cuid: "cuid",
    cuid2: "cuid2",
    ulid: "ULID",
    xid: "XID",
    ksuid: "KSUID",
    datetime: "تاريخ ووقت بفE�يار ISO",
    date: "تاريخ بفE�يار ISO",
    time: "وقت بفE�يار ISO",
    duration: "فE�ة بفE�يار ISO",
    ipv4: "عفE�افEIPv4",
    ipv6: "عفE�افEIPv6",
    cidrv4: "فE�فEعفE�ويفEبصيغة IPv4",
    cidrv6: "فE�فEعفE�ويفEبصيغة IPv6",
    base64: "فE�ص بترفE�ز base64-encoded",
    base64url: "فE�ص بترفE�ز base64url-encoded",
    json_string: "فE�ص عفE�EفE�ئة JSON",
    e164: "رقفEفE�تفEبفE�يار E.164",
    jwt: "JWT",
    template_literal: "فE�خفE,
  };

  const TypeDictionary: {
    [k in errors.$ZodInvalidTypeExpected | (string & {})]?: string;
  } = {
    nan: "NaN",
  };

  return (issue) => {
    switch (issue.code) {
      case "invalid_type": {
        const expected = TypeDictionary[issue.expected] ?? issue.expected;
        const receivedType = util.parsedType(issue.input);
        const received = TypeDictionary[receivedType] ?? receivedType;
        if (/^[A-Z]/.test(issue.expected)) {
          return `فE�خفE�ت غير فE�بوفE�: يفE�رض إدخافEinstanceof ${issue.expected}؁EوفE�E�EتفEإدخافE${received}`;
        }
        return `فE�خفE�ت غير فE�بوفE�: يفE�رض إدخافE${expected}؁EوفE�E�EتفEإدخافE${received}`;
      }
      case "invalid_value":
        if (issue.values.length === 1)
          return `فE�خفE�ت غير فE�بوفE�: يفE�رض إدخافE${util.stringifyPrimitive(issue.values[0])}`;
        return `اختيار غير فE�بوفE يتوقع افE�قاء أحد فE�فEافE�يارات: ${util.joinValues(issue.values, "|")}`;
      case "too_big": {
        const adj = issue.inclusive ? "<=" : "<";
        const sizing = getSizing(issue.origin);
        if (sizing)
          return ` أفE�ر فE�EافE�E�زفE يفE�رض أفEتفE�فE${issue.origin ?? "افE�يفE�"} ${adj} ${issue.maximum.toString()} ${sizing.unit ?? "عفE�ر"}`;
        return `أفE�ر فE�EافE�E�زفE يفE�رض أفEتفE�فE${issue.origin ?? "افE�يفE�"} ${adj} ${issue.maximum.toString()}`;
      }
      case "too_small": {
        const adj = issue.inclusive ? ">=" : ">";
        const sizing = getSizing(issue.origin);
        if (sizing) {
          return `أصغر فE�EافE�E�زفE يفE�رض فE� ${issue.origin} أفEيفE�فE${adj} ${issue.minimum.toString()} ${sizing.unit}`;
        }

        return `أصغر فE�EافE�E�زفE يفE�رض فE� ${issue.origin} أفEيفE�فE${adj} ${issue.minimum.toString()}`;
      }
      case "invalid_format": {
        const _issue = issue as errors.$ZodStringFormatIssues;
        if (_issue.format === "starts_with") return `فE�ص غير فE�بوفE يجب أفEيبدأ بـ "${issue.prefix}"`;
        if (_issue.format === "ends_with") return `فE�ص غير فE�بوفE يجب أفEيفE�فE�Eبـ "${_issue.suffix}"`;
        if (_issue.format === "includes") return `فE�ص غير فE�بوفE يجب أفEيتضفE�َفE"${_issue.includes}"`;
        if (_issue.format === "regex") return `فE�ص غير فE�بوفE يجب أفEيطابفEافE�E�E� ${_issue.pattern}`;
        return `${FormatDictionary[_issue.format] ?? issue.format} غير فE�بول`;
      }
      case "not_multiple_of":
        return `رقفEغير فE�بوفE يجب أفEيفE�فEفE�EفE�اعفE�ت ${issue.divisor}`;
      case "unrecognized_keys":
        return `فE�رفE{issue.keys.length > 1 ? "ات" : ""} غريب${issue.keys.length > 1 ? "ة" : ""}: ${util.joinValues(issue.keys, "؁E")}`;
      case "invalid_key":
        return `فE�رفEغير فE�بوفEفE�E${issue.origin}`;
      case "invalid_union":
        return "فE�خفEغير فE�بوفE;
      case "invalid_element":
        return `فE�خفEغير فE�بوفEفE�E${issue.origin}`;
      default:
        return "فE�خفEغير فE�بوفE;
    }
  };
};

export default function (): { localeError: errors.$ZodErrorMap } {
  return {
    localeError: error(),
  };
}
