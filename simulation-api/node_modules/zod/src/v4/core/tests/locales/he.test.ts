import { beforeEach, describe, expect, test } from "vitest";
import { z } from "../../../../index.js";
import he from "../../../locales/he.js";

describe("Hebrew localization", () => {
  beforeEach(() => {
    z.config(he());
  });

  describe("too_small errors with definite article and gendered verbs", () => {
    test("string type (feminine - צריכׁE", () => {
      const schema = z.string().min(3);
      const result = schema.safeParse("ab");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קצר מדׁE המחרוזת צריכׁEלהכיׁE3 תוויׁEאׁEיותר");
      }
    });

    test("number type (masculine - צריׁE", () => {
      const schema = z.number().min(10);
      const result = schema.safeParse(5);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קטׁEמדׁE המספר צריׁEלהיות גדוׁEאׁEשווׁEׁE10");
      }
    });

    test("array type (masculine - צריׁE", () => {
      const schema = z.array(z.string()).min(1);
      const result = schema.safeParse([]);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קטׁEמדׁE המערׁEצריׁEלהכיׁEלפחות פריׁEאחׁE);
      }
    });

    test("set type (feminine - צריכׁE", () => {
      const schema = z.set(z.string()).min(2);
      const result = schema.safeParse(new Set(["a"]));
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קטׁEמדׁE הקבוצׁE(Set) צריכׁEלהכיׁE2 פריטיׁEאׁEיותר");
      }
    });
  });

  describe("too_big errors with definite article and gendered verbs", () => {
    test("string type (feminine - צריכׁE", () => {
      const schema = z.string().max(3);
      const result = schema.safeParse("abcde");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("ארוׁEמדׁE המחרוזת צריכׁEלהכיׁE3 תוויׁEאׁEפחות");
      }
    });

    test("number type (masculine - צריׁE", () => {
      const schema = z.number().max(365);
      const result = schema.safeParse(400);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("גדוׁEמדׁE המספר צריׁEלהיות קטׁEאׁEשווׁEׁE365");
      }
    });

    test("array max", () => {
      const schema = z.array(z.string()).max(2);
      const result = schema.safeParse(["a", "b", "c"]);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("גדוׁEמדׁE המערׁEצריׁEלהכיׁE2 פריטיׁEאׁEפחות");
      }
    });
  });

  describe("invalid_type errors with definite article and gendered verbs", () => {
    test("string expected (feminine), number received", () => {
      const schema = z.string();
      const result = schema.safeParse(123);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות מחרוזת, התקבׁEמספר");
      }
    });

    test("number expected (masculine), string received", () => {
      const schema = z.number();
      const result = schema.safeParse("abc");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות מספר, התקבׁEמחרוזת");
      }
    });

    test("boolean expected (masculine), null received", () => {
      const schema = z.boolean();
      const result = schema.safeParse(null);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות ערׁEבוליאנׁE התקבׁEערׁEריק (null)");
      }
    });

    test("array expected (masculine), object received", () => {
      const schema = z.array(z.string());
      const result = schema.safeParse({});
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות מערׁE התקבׁEאובייקׁE);
      }
    });

    test("object expected (masculine), array received", () => {
      const schema = z.object({ a: z.string() });
      const result = schema.safeParse([]);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות אובייקׁE התקבׁEמערׁE);
      }
    });

    test("function expected (feminine), string received", () => {
      const schema = z.function();
      const result = schema.safeParse("not a function");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות פונקציׁE התקבׁEמחרוזת");
      }
    });
  });

  describe("gendered verbs consistency", () => {
    test("feminine types use צריכׁE, () => {
      const feminineTypes = [
        { schema: z.string().min(5), input: "abc" },
        { schema: z.set(z.string()).min(2), input: new Set(["a"]) },
      ];

      for (const { schema, input } of feminineTypes) {
        const result = schema.safeParse(input);
        expect(result.success).toBe(false);
        if (!result.success) {
          expect(result.error.issues[0].message).toContain("צריכׁE);
        }
      }
    });

    test("masculine types use צריׁE, () => {
      const masculineTypes = [
        { schema: z.number().min(10), input: 5 },
        { schema: z.array(z.string()).min(2), input: ["a"] },
      ];

      for (const { schema, input } of masculineTypes) {
        const result = schema.safeParse(input);
        expect(result.success).toBe(false);
        if (!result.success) {
          expect(result.error.issues[0].message).toContain("צריׁE);
        }
      }
    });
  });

  describe("invalid_value with enum", () => {
    test("single value", () => {
      const schema = z.enum(["a"]);
      const result = schema.safeParse("b");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('ערׁEלׁEתקיׁE הערׁEחייׁEלהיות "a"');
      }
    });

    test("two values", () => {
      const schema = z.enum(["a", "b"]);
      const result = schema.safeParse("c");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('ערׁEלׁEתקיׁE האפשרויות המתאימות הׁE"a" אׁE"b"');
      }
    });

    test("multiple values", () => {
      const schema = z.enum(["a", "b", "c"]);
      const result = schema.safeParse("d");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('ערׁEלׁEתקיׁE האפשרויות המתאימות הׁE"a", "b" אׁE"c"');
      }
    });
  });

  describe("other error types", () => {
    test("not_multiple_of", () => {
      const schema = z.number().multipleOf(3);
      const result = schema.safeParse(10);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("מספר לׁEתקיׁE חייׁEלהיות מכפלׁEשׁE3");
      }
    });

    test("unrecognized_keys - single key", () => {
      const schema = z.object({ a: z.string() }).strict();
      const result = schema.safeParse({ a: "test", b: "extra" });
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('מפתׁEלׁEמזוהׁE "b"');
      }
    });

    test("unrecognized_keys - multiple keys", () => {
      const schema = z.object({ a: z.string() }).strict();
      const result = schema.safeParse({ a: "test", b: "extra", c: "more" });
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('מפתחות לׁEמזוהיׁE "b", "c"');
      }
    });

    test("invalid_union", () => {
      const schema = z.union([z.string(), z.number()]);
      const result = schema.safeParse(true);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE);
      }
    });

    test("invalid_key in object", () => {
      const schema = z.record(z.number(), z.string());
      const result = schema.safeParse({ notANumber: "value" });
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("שדׁEלׁEתקיׁEבאובייקׁE);
      }
    });
  });

  describe("invalid_format with string checks", () => {
    test("startsWith", () => {
      const schema = z.string().startsWith("hello");
      const result = schema.safeParse("world");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('המחרוזת חייבת להתחיׁEׁE"hello"');
      }
    });

    test("endsWith", () => {
      const schema = z.string().endsWith("world");
      const result = schema.safeParse("hello");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('המחרוזת חייבת להסתייׁEׁE"world"');
      }
    });

    test("includes", () => {
      const schema = z.string().includes("test");
      const result = schema.safeParse("hello world");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe('המחרוזת חייבת לכלוׁE"test"');
      }
    });

    test("regex", () => {
      const schema = z.string().regex(/^[a-z]+$/);
      const result = schema.safeParse("ABC123");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("המחרוזת חייבת להתאיׁEלתבנית /^[a-z]+$/");
      }
    });
  });

  describe("invalid_format with common formats", () => {
    test("email", () => {
      const schema = z.string().email();
      const result = schema.safeParse("not-an-email");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("כתובת אימייׁEלׁEתקינׁE);
      }
    });

    test("url", () => {
      const schema = z.string().url();
      const result = schema.safeParse("not-a-url");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("כתובת רשת לׁEתקינׁE);
      }
    });

    test("uuid", () => {
      const schema = z.string().uuid();
      const result = schema.safeParse("not-a-uuid");
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("UUID לׁEתקיׁE);
      }
    });
  });

  describe("tuple validation", () => {
    test("invalid element type in tuple shows full error message", () => {
      const schema = z.tuple([z.string(), z.number()]);
      const result = schema.safeParse(["abc", "not a number"]);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קלׁEלׁEתקיׁE צריׁEלהיות מספר, התקבׁEמחרוזת");
      }
    });
  });

  describe("inclusive vs exclusive bounds", () => {
    test("inclusive minimum (>=)", () => {
      const schema = z.number().min(10);
      const result = schema.safeParse(5);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קטׁEמדׁE המספר צריׁEלהיות גדוׁEאׁEשווׁEׁE10");
      }
    });

    test("exclusive minimum (>)", () => {
      const schema = z.number().gt(10);
      const result = schema.safeParse(10);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("קטׁEמדׁE המספר צריׁEלהיות גדוׁEׁE10");
      }
    });

    test("inclusive maximum (<=)", () => {
      const schema = z.number().max(10);
      const result = schema.safeParse(15);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("גדוׁEמדׁE המספר צריׁEלהיות קטׁEאׁEשווׁEׁE10");
      }
    });

    test("exclusive maximum (<)", () => {
      const schema = z.number().lt(10);
      const result = schema.safeParse(10);
      expect(result.success).toBe(false);
      if (!result.success) {
        expect(result.error.issues[0].message).toBe("גדוׁEמדׁE המספר צריׁEלהיות קטׁEׁE10");
      }
    });
  });

  describe("all type names with definite article", () => {
    test("verifies all type translations are correct", () => {
      const types = [
        { schema: z.string(), expected: "מחרוזת", input: 123 },
        { schema: z.number(), expected: "מספר", input: "abc" },
        { schema: z.boolean(), expected: "ערׁEבוליאנׁE, input: "abc" },
        { schema: z.bigint(), expected: "BigInt", input: "abc" },
        { schema: z.date(), expected: "תאריׁE, input: "abc" },
        { schema: z.array(z.any()), expected: "מערׁE, input: "abc" },
        { schema: z.object({}), expected: "אובייקׁE, input: "abc" },
        { schema: z.function(), expected: "פונקציׁE, input: "abc" },
      ];

      for (const { schema, expected, input } of types) {
        const result = schema.safeParse(input);
        expect(result.success).toBe(false);
        if (!result.success) {
          expect(result.error.issues[0].message).toContain(expected);
        }
      }
    });
  });
});
