import * as util from "../core/util.js";
function getRussianPlural(count, one, few, many) {
    const absCount = Math.abs(count);
    const lastDigit = absCount % 10;
    const lastTwoDigits = absCount % 100;
    if (lastTwoDigits >= 11 && lastTwoDigits <= 19) {
        return many;
    }
    if (lastDigit === 1) {
        return one;
    }
    if (lastDigit >= 2 && lastDigit <= 4) {
        return few;
    }
    return many;
}
const error = () => {
    const Sizable = {
        string: {
            unit: {
                one: "сE�мвол",
                few: "сE�мвола",
                many: "сE�мволов",
            },
            verb: "иметсE,
        },
        file: {
            unit: {
                one: "байсE,
                few: "байта",
                many: "байсE,
            },
            verb: "иметсE,
        },
        array: {
            unit: {
                one: "элеменсE,
                few: "элемента",
                many: "элементов",
            },
            verb: "иметсE,
        },
        set: {
            unit: {
                one: "элеменсE,
                few: "элемента",
                many: "элементов",
            },
            verb: "иметсE,
        },
    };
    function getSizing(origin) {
        return Sizable[origin] ?? null;
    }
    const FormatDictionary = {
        regex: "ввод",
        email: "email адресE,
        url: "URL",
        emoji: "эмодзи",
        uuid: "UUID",
        uuidv4: "UUIDv4",
        uuidv6: "UUIDv6",
        nanoid: "nanoid",
        guid: "GUID",
        cuid: "cuid",
        cuid2: "cuid2",
        ulid: "ULID",
        xid: "XID",
        ksuid: "KSUID",
        datetime: "ISO дата и времсE,
        date: "ISO дата",
        time: "ISO времсE,
        duration: "ISO длительносE�сE,
        ipv4: "IPv4 адресE,
        ipv6: "IPv6 адресE,
        cidrv4: "IPv4 диапазон",
        cidrv6: "IPv6 диапазон",
        base64: "сE�рока в сE�рмате base64",
        base64url: "сE�рока в сE�рмате base64url",
        json_string: "JSON сE�рока",
        e164: "номер E.164",
        jwt: "JWT",
        template_literal: "ввод",
    };
    const TypeDictionary = {
        nan: "NaN",
        number: "сE�сE�о",
        array: "масE�E�в",
    };
    return (issue) => {
        switch (issue.code) {
            case "invalid_type": {
                const expected = TypeDictionary[issue.expected] ?? issue.expected;
                const receivedType = util.parsedType(issue.input);
                const received = TypeDictionary[receivedType] ?? receivedType;
                if (/^[A-Z]/.test(issue.expected)) {
                    return `Неверный ввод: ожидалосE�Einstanceof ${issue.expected}, полусE�но ${received}`;
                }
                return `Неверный ввод: ожидалосE�E${expected}, полусE�но ${received}`;
            }
            case "invalid_value":
                if (issue.values.length === 1)
                    return `Неверный ввод: ожидалосE�E${util.stringifyPrimitive(issue.values[0])}`;
                return `Неверный вариансE ожидалосE�Eодно из ${util.joinValues(issue.values, "|")}`;
            case "too_big": {
                const adj = issue.inclusive ? "<=" : "<";
                const sizing = getSizing(issue.origin);
                if (sizing) {
                    const maxValue = Number(issue.maximum);
                    const unit = getRussianPlural(maxValue, sizing.unit.one, sizing.unit.few, sizing.unit.many);
                    return `Слишком большое знасE�ние: ожидалосE�E сE�о ${issue.origin ?? "знасE�ние"} будесEиметсE${adj}${issue.maximum.toString()} ${unit}`;
                }
                return `Слишком большое знасE�ние: ожидалосE�E сE�о ${issue.origin ?? "знасE�ние"} будесE${adj}${issue.maximum.toString()}`;
            }
            case "too_small": {
                const adj = issue.inclusive ? ">=" : ">";
                const sizing = getSizing(issue.origin);
                if (sizing) {
                    const minValue = Number(issue.minimum);
                    const unit = getRussianPlural(minValue, sizing.unit.one, sizing.unit.few, sizing.unit.many);
                    return `Слишком маленькое знасE�ние: ожидалосE�E сE�о ${issue.origin} будесEиметсE${adj}${issue.minimum.toString()} ${unit}`;
                }
                return `Слишком маленькое знасE�ние: ожидалосE�E сE�о ${issue.origin} будесE${adj}${issue.minimum.toString()}`;
            }
            case "invalid_format": {
                const _issue = issue;
                if (_issue.format === "starts_with")
                    return `НевернасEсE�рока: должна насE�натьсE�EсE"${_issue.prefix}"`;
                if (_issue.format === "ends_with")
                    return `НевернасEсE�рока: должна закансE�ватьсE�Eна "${_issue.suffix}"`;
                if (_issue.format === "includes")
                    return `НевернасEсE�рока: должна сE�держатсE"${_issue.includes}"`;
                if (_issue.format === "regex")
                    return `НевернасEсE�рока: должна сE�ответсE�воватсEшаблонсE${_issue.pattern}`;
                return `Неверный ${FormatDictionary[_issue.format] ?? issue.format}`;
            }
            case "not_multiple_of":
                return `Неверное сE�сE�о: должно бытсEкратным ${issue.divisor}`;
            case "unrecognized_keys":
                return `НерасE�ознанн${issue.keys.length > 1 ? "ые" : "ый"} клюсE{issue.keys.length > 1 ? "и" : ""}: ${util.joinValues(issue.keys, ", ")}`;
            case "invalid_key":
                return `Неверный клюсEв ${issue.origin}`;
            case "invalid_union":
                return "Неверные всE�дные данные";
            case "invalid_element":
                return `Неверное знасE�ние в ${issue.origin}`;
            default:
                return `Неверные всE�дные данные`;
        }
    };
};
export default function () {
    return {
        localeError: error(),
    };
}
