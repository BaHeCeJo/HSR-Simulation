import type {Vocabulary} from "../../types"
import idKeyword from "./id.js"
import refKeyword from "./ref.js"

const core: Vocabulary = [
  "$schema",
  "$id",
  "$defs",
  "$vocabulary",
  {keyword: "$comment"},
  "definitions",
  idKeyword,
  refKeyword,
]

export default core
